console.clear();

function dutyFree(normPrice, discount, hol){
    let savings = 10 * (100% * 10%); 
      let holiday = hol / savings;
      return holiday.toFixed(0);
    }

    return dutyFree(savings);

    //negerai